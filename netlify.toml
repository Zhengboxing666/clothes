[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 环境变量配置（在 Netlify 控制台中设置）
# VITE_SUPABASE_URL = "您的 Supabase 项目 URL"
# VITE_SUPABASE_ANON_KEY = "您的 Supabase Anon Key"